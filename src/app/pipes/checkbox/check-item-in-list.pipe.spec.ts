import { CheckItemInListPipe } from './check-item-in-list.pipe';

describe('CheckItemInListPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckItemInListPipe();
    expect(pipe).toBeTruthy();
  });
});
